name: project2
on:
  push:
    branches:
      - "*"
  workflow_dispatch:

env:
  POSTGRES_DB: project2
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_HOST: postgres

# GOOGLE_CLIENT_ID=842817349023-qu09spp8iuac5rhni5k0vh5sav2c3it2.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=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

# APP_ID=694160789064603
# APP_SECRET=534fbd133c76398edc1ee0c9ba7622e8
# RECIPIENT_WAID=85251170071
# VERSION=v16.0
# PHONE_NUMBER_ID=101078972951844
# ACCESS_TOKEN=EAAJ3VecZCy5sBAHLm8ZB8ZAUHW0gB2ZA2itredpI0kVbeZCJbTXcll0rfBYZCFd7aWLy0c85ZBWs6973OpZBXN3etZAnaEh1LtZAvLEpEzzWC2tAmF3rXZC3Y0GpBpArjNalCp2ZB00buDDHBa3PMTqhw9BM79p2VGAlUkBQ7BvtpLPsA2anJdPRDJ1t1ONu989XdF6zDZBeYALJUD0a2ZCawCyH7lDm7q4KfQTZAQZD

jobs:
  testing:
    runs-on: ubuntu-latest
    container: node:latest
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_DB: ${{env.POSTGRES_DB}}
          POSTGRES_PASSWORD: ${{env.POSTGRES_PASSWORD}}
        ports:
          - 5432:5432
    steps:
      - uses: actions/checkout@v3
      - run: |
          yarn install 
          yarn knex migrate:latest --env test
          yarn jest
  deploy:
  if: github.ref_name == 'production'
    runs-on: ubuntu-latest
    needs: testing
    steps:
      - name: ssh to pull latest version
        uses: appleboy/ssh-action@master
        with:
          script: |
            cd c25-wsp-project-02-tw
            git pull origin main
            yarn install
            yarn knex migrate:latest --env production
            forever restart -c 'yarn start'
          host: 44.199.150.85
          username: ubuntu
          port: 22
          key: ${{secrets.SSH_PRIVATE_KEY}}